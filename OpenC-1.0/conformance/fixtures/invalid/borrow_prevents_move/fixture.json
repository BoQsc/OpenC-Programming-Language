{
  "active_rules": [
    "OPENC-BORROW-NOMOVE-001"
  ],
  "evidence_state": "EXECUTED_PASS_WINDOWS_X86_64_RC9",
  "expected": {
    "class": "invalid",
    "edition": "OpenC Core 1.0 Current",
    "environment": "hosted",
    "expected": {
      "diagnostic_rule": "OPENC-OWN-EXIT-001",
      "result": "reject",
      "rule": "OPENC-BORROW-NOMOVE-001"
    },
    "fixture_kind": "source",
    "mode": "module",
    "name": "borrow_prevents_move",
    "profile": "standard",
    "rules": [
      "OPENC-BORROW-NOMOVE-001"
    ]
  },
  "id": "invalid/borrow_prevents_move",
  "kind": "invalid",
  "origin": "OpenC Standard Draft 0.10 validation/invalid/borrow_prevents_move",
  "schema": "openc.fixture.v1",
  "source_files": [
    "conformance/fixtures/invalid/borrow_prevents_move/main.p"
  ]
}
