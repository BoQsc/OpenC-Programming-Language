# Roadmap from the source-complete mainline

```text
★ authored implementation source complete by inventory
        ↓
independent grammar, semantic, security, ABI, and usability review
        ↓
compile canonical D compiler/runtime/library/tools
        ↓
resolve every compiler/linker finding against the standard
        ↓
execute implementation gates I0–I5
        ↓
execute complete Core and component conformance corpora
        ↓
run maintained programs on claimed Linux/Windows/freestanding targets
        ↓
close all P0/P1 specification and implementation findings
        ↓
complete licensing, governance, signing, and release authority
        ↓
OpenC 1.0 release candidate
        ↓
clean rebuild, archive verification, and release audit
        ↓
OpenC 1.0
```

Future language work follows the proposal and accepted-change process. Provisional concurrency remains outside the Core 1.0 blocking path.
