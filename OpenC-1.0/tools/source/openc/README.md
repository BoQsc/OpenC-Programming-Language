# openc implementation source

Reusable implementations live in `tools/source/openc/tools/` and standalone application entry points live in `tools/source/apps/`.

The public self-hosted CLI is implemented under
`compiler/selfhost/source/`. SH-10 covers native `fmt`, `info`, and `test`;
the D implementation remains an auditable retained implementation.

Status: **OPENC-NATIVE PROJECT WORKFLOW BUILT AND TESTED ON WINDOWS; D
IMPLEMENTATION RETAINED**
