# formatter implementation source

Reusable implementations live in `tools/source/openc/tools/` and standalone application entry points live in `tools/source/apps/`.

The public self-hosted formatter is implemented in
`compiler/selfhost/source/cli_format.p` and
`compiler/selfhost/source/cli_project_workflow.p`.

Status: **OPENC-NATIVE IMPLEMENTATION BUILT AND TESTED ON WINDOWS; D
IMPLEMENTATION RETAINED**
