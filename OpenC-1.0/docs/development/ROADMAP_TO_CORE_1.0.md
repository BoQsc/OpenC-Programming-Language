# IR1 to OpenC Core 1.0

```text
IR1 implementation-ready authored baseline
    ↓
I0 complete lexer/parser/AST + all grammar fixtures
    ↓
I1 names/types/constants/overloads
    ↓
I2 flow/optional/status-out
    ↓
I3 ownership/resources/cleanup/borrows
    ↓
I4 storage/pointers/unsafe/provenance
    ↓
I5 interpreter or native backend
    ↓
complete Core conformance execution
    ↓
three maintained real Core programs
    ↓
independent grammar, semantic, security, and usability review
    ↓
all findings corrected and re-executed
    ↓
OpenC Core 1.0 RC
    ↓
license/governance/release authorization
    ↓
OpenC Core 1.0
```

Hosted, Freestanding, Native, Tooling, script/live, and Concurrent claims remain separately versioned work. They do not silently redefine Core and do not block beginning the Core implementation.
