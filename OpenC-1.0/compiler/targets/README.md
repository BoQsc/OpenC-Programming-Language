# OpenC target records

The authored target records describe the minimum facts used by the compiler and build tools. They are provider inputs, not proof that any target has been compiled or executed.

```text
linux-x86_64.json
windows-x86_64.json
freestanding-x86_64.json
```
