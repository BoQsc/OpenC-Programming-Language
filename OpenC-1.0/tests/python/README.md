# Python bootstrap tests

These standard-library-only tests exercise the informative Python bootstrap source. They are authored but have not been executed in this package.

A future maintainer may run them with:

```text
PYTHONPATH=compiler/bootstrap/python python -m unittest discover -s tests/python
```
