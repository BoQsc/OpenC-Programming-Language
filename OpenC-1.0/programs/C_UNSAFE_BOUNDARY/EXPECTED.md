# Expected result

The program is valid Core and returns `30` on a target supporting aligned `i32` access. It exercises typed storage, stable construct placement, scope destruction, address-of, provenance, raw write, and a safe read wrapper.

Evidence state: `EXECUTED_PASS_WINDOWS_BOOTSTRAP` (exit code `30`).
