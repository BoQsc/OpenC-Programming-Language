# Expected result

The program prints the process argument count followed by every argument and returns `0`.

It exercises the minimum Hosted boundary:

```text
system.io
system.process
text output
usize arithmetic
while control flow
```

Evidence state: `EXECUTION_PENDING`.
