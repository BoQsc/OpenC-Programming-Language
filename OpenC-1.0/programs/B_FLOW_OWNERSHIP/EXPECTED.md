# Expected result

The program is self-contained Core and returns `0`. It exercises status/out lineage, resource output publication, a slot-free resource obligation, domain-specific movement contract availability, and consuming scope cleanup.

Evidence state: `EXECUTED_PASS_WINDOWS_BOOTSTRAP` (exit code `0`).
