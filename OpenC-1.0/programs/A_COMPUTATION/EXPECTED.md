# Expected result

The program is valid Core and returns `32`. It exercises direct functions, struct initialization, enum switch, exact integer arithmetic, and exhaustiveness.

Evidence state: `EXECUTION_PENDING`.
