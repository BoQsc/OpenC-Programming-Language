# Security Maintenance

Security reports should identify the rule or contract that fails, a minimal reproducer, affected targets/implementations, and whether the issue permits safe-code memory/type/lifetime violation, arbitrary optimizer behavior, or conformance misreporting.

A security correction cannot be hidden as implementation-specific behavior. The standard, security model, fixtures, implementation, and compatibility record are updated together.
