
# Versioning and compatibility

```text
patch release
    clarifications, errata, implementation fixes; no intentional valid-source break

minor release
    additive features and explicitly classified compatible changes

major release
    intentional breaking grammar or semantic changes
```

Every normative change records old version, new version, compatibility class, affected rule IDs, profiles/components, fixtures, and migration guidance.
