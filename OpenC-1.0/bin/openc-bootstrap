#!/usr/bin/env sh
set -eu
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHON=${PYTHON:-python3}
PYTHONPATH="$ROOT/compiler/bootstrap/python${PYTHONPATH:+:$PYTHONPATH}" \
  exec "$PYTHON" -m openc "$@"
